for i in range(1,11):
    print(f"5 X {i} = {5*1}")
    """
    What is an f-string?

The f tells Python:

“This is a formatted string. 
Evaluate the expressions inside { } first.”

So Python replaces the values inside {} before printing.
    """